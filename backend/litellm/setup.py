from setuptools import setup, find_packages

setup(
    name="litellm",
    version="1.0.0",
    packages=find_packages(),
    description="Local stub package for litellm used in repo for development",
)
